{
	"name": "@egostan/www",
	"version": "0.0.1",
	"description": "",
	"author": "Stanislav S. Yarmonov",
	"license": "MIT",
	"scripts": {
		"clean:next": "rimraf .next",
		"clean:export": "rimraf .export",
		"clean": "yarn clean:next && yarn clean:export",
		"dev": "next",
		"build": "next build",
		"start": "next start",
		"export": "yarn clean && yarn build && next export -o .export"
	},
	"dependencies": {
		"@zeit/next-css": "^1.0.1",
		"@zeit/next-typescript": "^1.1.1",
		"next": "^8.1.0",
		"normalize.css": "^8.0.1",
		"react": "^16.8.6",
		"react-dom": "^16.8.6"
	},
	"devDependencies": {
		"@types/next": "^8.0.5",
		"rimraf": "^2.6.3"
	}
}
